# AI 伴游 - 未来研发规划 (V4 Roadmap)

在历经 V1 至 V3 阶段的迭代后，我们已经通过纯前端技术（HTML/CSS/Vanilla JS）完成了高保真、极具沉浸感的概念验证（Proof of Concept, PoC）Demo。
该 Demo 已经涵盖了**多模态交互**、**多场景适配（西湖/故宫/商业空间）**、**TTS 语音解说**、以及**动态数字人引擎**等核心特性。

为了将该 PoC 转化为真正可面向终端用户的生产级产品，接下来的研发阶段需从以下四个维度进行全方位升级：

## 阶段一：技术栈与架构重构 (Foundation & Architecture)
当前的 Vanilla JS 代码随着迭代已逐渐庞大，难以进行复杂的状态管理和多人协作。
1. **拥抱现代前端框架**
   - **选型建议**：React + TypeScript (如使用 Next.js 或 Vite)，或者直接使用 **支付宝/微信小程序原生框架**（考虑到伴游类应用多为扫码即用的轻量级场景）。
   - **状态管理**：引入 Zustand 或 Redux 来全局管理 `游览状态`、`消息列表`、`数字人状态`、`场景数据`，彻底解耦 UI 渲染与业务逻辑。
2. **组件化与工程化**
   - 提取 `<ChatBox/>`、`<MapLayer/>`、`<AvatarWidget/>`、`<VirtualCamera/>` 等独立组件。
   - 使用 TailwindCSS 替代目前的纯 CSS 手写规范，以加快迭代和多端适配（如深色模式支持）。

## 阶段二：接入真实生态 SDK (Ecosystem Integration)
Demo 中的地图游览、GPS 坐标、照片识别等均为 Mock 数据，需要在下一阶段接入真实能力。
1. **真实地图与 LBS 引擎**
   - 摈弃 Canvas 绘图，接入 **高德地图 / 百度地图 / 腾讯地图 JS API**。
   - 启用设备的真实 `Geolocation API` 并绑定地图视角，实现真实的地理围栏计算（Geofencing）和步道偏移检测。
2. **打通真实 AI 大模型接口**
   - **流式对话**：接入诸如 通义千问 / 豆包 / 智谱 等底层 LLM。改用 WebSocket/SSE 获取流式输出（打字机效果无缝衔接）。
   - **视觉大模型 (VLM)**：将摄像头捕获的 `Base64` 图像上传至后端调用视觉模型（如 Qwen-VL 等），替代目前随机回复的占位逻辑。
3. **商业级语音接入 (TTS & ASR)**
   - **TTS**：使用云端的高质量神经拟声源（如阿里云/火山引擎的定制数字人音源），彻底解决浏览器原生 SpeechSynthesis 音质不一、中断、口音问题。
   - **ASR**：接入语音转文字识别，允许用户长按麦克风进行真实语音提问输入。

## 阶段三：数字人与 3D 渲染升级 (Avatar 2.0)
当前的数字人为 2D CSS 渲染，对于极致体验仍有提升空间。
1. **进阶动画体系**：使用 WebGL / Three.js 或 Lottie 渲染带骨骼动画的 2D/3D 数字人。
2. **音唇同步 (Lip-sync)**：结合云端 TTS 接口下发的音素/时间戳，实现精准的唇形匹配，而非简单的张嘴闭嘴循环。

## 阶段四：运营与商业化平台 (CMS & Backend)
走向生产意味着景区数据不能硬编码。
1. **全动态场景下发**：开发后台管理系统 (CMS)，令景区管理员可自行在地图上框选多边形，配置 POI 与导览词，随时更新活动推送卡片。
2. **数据沉淀与长线玩法**：集成用户系统，加入云端旅游打卡护照、收集数字徽章激励机制。
